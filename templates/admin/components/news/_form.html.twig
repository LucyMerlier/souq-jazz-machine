{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-{{ button_label is defined ? 'warning' : 'success' }}">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
