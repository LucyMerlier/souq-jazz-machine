<form method="post" action="{{ path('admin_song_delete', {'id': song.id}) }}" onsubmit="return confirm('Es-tu certain·e de vouloir supprimer ce morceau? Cette action est irréversible');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ song.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
