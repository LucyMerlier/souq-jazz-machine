{% extends "admin/layout.html.twig" %}

{% block title %}Modifier la partition - {{ parent() }}{% endblock %}

{% block content %}
    <h1 class="h1 my-4">Modifier la partition :</h1>

    <div class="my-4">
        <a href="{{ path('admin_song_edit', {'id': sheet.song.id}) }}">&lt;- Revenir sur le morceau</a>
    </div>

    {% include "admin/song/components/_sheet_form.html.twig" with {"button_label": "Modifier"} %}
    <div class="my-1">
        {% include "admin/song/components/_sheet_delete_form.html.twig" %}
    </div>
{% endblock %}
