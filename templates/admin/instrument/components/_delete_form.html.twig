<form method="post" action="{{ path('admin_instrument_delete', {'id': instrument.id}) }}" onsubmit="return confirm('Es-tu certain·e de vouloir supprimer cet instrument? Cette action est irréversible');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instrument.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash me-2"></i>Supprimer</button>
</form>
