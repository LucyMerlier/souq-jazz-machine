{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-{{ button_label is defined ? 'warning' : 'success' }}"><i class="fas fa-{{ button_label is defined ? 'pen' : 'plus' }} me-2"></i>{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
