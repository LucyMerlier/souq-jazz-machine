<nav class="navbar navbar-expand-xl navbar-light bg-light">
    <div class="container-fluid container">
        <a class="navbar-brand" href="{{ path('showcase_home') }}">&lt;- Retour au site vitrine</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'admin_home' ? 'active' }}" href="{{ path('admin_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'admin_concert_agenda' ? 'active' }}" href="{{ path('admin_concert_agenda') }}">Concerts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'admin_songs' ? 'active' }}" href="{{ path('admin_songs') }}">Partitions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'admin_members' ? 'active' }}" href="{{ path('admin_members') }}">Membres</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href="#">Contacts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'admin_albums' ? 'active' }}" href="{{ path('admin_albums') }}">Médias</a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="admin-tasks-dropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Admin
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="admin-tasks-dropdown">
                            <li>
                                <a class="dropdown-item" href="{{ path('admin_news_index') }}">Gestion des actus</a>
                            </li>
                            <li>
                                <a class="dropdown-item disabled" href="#">Gestion du contenu</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('admin_instrument_index') }}">Gestion des instruments</a>
                            </li>
                            <li>
                                <a class="dropdown-item disabled" href="#">Gestion des petites annonces</a>
                            </li>
                        </ul>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="my-account-dropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Mon compte
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="my-account-dropdown">
                        <li class="dropdown-item">
                            {{ app.user.pseudonym ?? app.user.firstname }}
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('admin_user_show') }}">Mes infos</a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Me déconnecter</a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
