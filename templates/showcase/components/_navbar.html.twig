<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('showcase_home') }}">SOUQ' JAZZ MACHINE</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_home' ? 'active' }}" href="{{ path('showcase_home') }}">ACCUEIL</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_about_us' ? 'active' }}" href="{{ path('showcase_about_us') }}">LE BIG BAND</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">GALLERIE</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_contact' ? 'active' }}" href="{{ path('showcase_agenda') }}">CALENDRIER</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_contact' ? 'active' }}" href="{{ path('showcase_contact') }}">CONTACT</a>
                </li>
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            ADMIN
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li class="dropdown-item">
                                {{ app.user.pseudonym ?? app.user.firstname }}
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('admin_home') }}">Retour au site admin</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('admin_user_show') }}">Mes infos</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_logout') }}">Me déconnecter</a>
                            </li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
