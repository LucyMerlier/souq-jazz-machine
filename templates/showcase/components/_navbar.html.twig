<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('showcase_home') }}">SOUQ' JAZZ MACHINE</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_home' ? 'active' }}" href="{{ path('showcase_home') }}">ACCUEIL</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_about_us' ? 'active' }}" href="{{ path('showcase_about_us') }}">LE BIG BAND</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">GALLERIE</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">CALENDRIER</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') == 'showcase_contact' ? 'active' }}" href="{{ path('showcase_contact') }}">CONTACT</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
