{% extends "showcase/layout.html.twig" %}

{% block title %}Répondre à l'annonce - {{ parent() }}{% endblock %}

{% block content %}
    <h1 data-aos="fade-right" class="h1 my-4">REJOIGNEZ-NOUS!</h1>

    <h2 data-aos="fade-right" class="h3 my-4">{{ offer.description }}</h2>


    <div data-aos="fade-up">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-secondary"><i class="fas fa-paper-plane me-2"></div>Envoyer!</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
